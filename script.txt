CREATE TABLE USUARIOS(
IDUSUARIO int primary key AUTO_INCREMENT,
CI VARCHAR(20) NOT NULL,
NOMBRES VARCHAR(60) NOT NULL,
APELLIDOS VARCHAR(60) NOT NULL,
TELEFONO VARCHAR(20),
CORREO VARCHAR(60),
CONTRASENA VARCHAR(20),
ROL CHAR(1),
ESTADO VARCHAR(10) DEFAULT 'NORMAL',
EJEX VARCHAR(50) NOT NULL,
EJEY VARCHAR(50) NULL,
ACTIVO CHAR(1) default '1'
)

DONDE ROL
1: USUARIO NORMAL
2: ADMINISTRADOR

DONDE ESTADO
NORMAL: PERSONA SIN CORONAVIRUS
ACTIVO: PORTADOR DE CORONAVIRUS
RECUPERADO: PERSONA QUE SE CURO DEL CORONAVIRUS
FALLECIDO: PERSONA QUE MURIO CON CORONAVIRUS

INSERT INTO USUARIOS(CI,NOMBRES,APELLIDOS,TELEFONO,CORREO,CONTRASENA,ROL,EJEX,EJEY)VALUES('1033','ALVA','ZAP','793','V@GMAIL.COM','2','-19.0241477','-65.280443')

CREATE TABLE UBICACIONES(
IDUBICACION int primary key AUTO_INCREMENT,
NOMBRE varchar(60) not null,
Descripcion varchar(200) not null,
Gravedad varchar(10),
EJEX VARCHAR(50) NOT NULL,
EJEY VARCHAR(50) NOT NULL,
ACTIVO CHAR(1) default '1'
)
INSERT INTO UBICACIONES(NOMBRE,DESCRIPCION,GRAVEDAD,EJEX,EJEY)VALUES('MERCADO CAMPESINO','MERCADO CON ALTA INCURRENCIA','ALTO','-19.0251','-65.2164')

CREATE TABLE UBICACIONUSUARIOS(
IDUBICACIONUSUARIO int primary key AUTO_INCREMENT,
FECHA DATETIME NOT NULL,
ACTIVO CHAR(1) default '1',
IDUSUARIO INT NOT NULL,
IDUBICACION INT NOT NULL,
FOREIGN KEY(IDUSUARIO)REFERENCES USUARIOS(IDUSUARIO),
FOREIGN KEY(IDUBICACION)REFERENCES UBICACIONES(IDUBICACION)
)
INSERT INTO UBICACIONUSUARIOS(FECHA,IDUSUARIO,IDUBICACION)VALUES(NOW(),1,1)

CREATE TABLE HOSPITALES(
IDHOSPITAL INT PRIMARY KEY AUTO_INCREMENT,
NOMBRE VARCHAR(60),
EJEX VARCHAR(50) NOT NULL,
EJEY VARCHAR(50) NOT NULL,
DESCRIPCION VARCHAR(200),
ACTIVO CHAR(1) default '1'
)
INSERT INTO HOSPITALES(NOMBRE,EJEX,EJEY,DESCRIPCION)VALUES('IPTK','-19.040979','-65.2592543','ESTO ES UNA DESCRIPCION')
INSERT INTO HOSPITALES(NOMBRE,EJEX,EJEY,DESCRIPCION)VALUES('HOSPITAL UNIVERSITARIO','-19.0283621','-65.2592141','ESTO ES UNA DESCRIPCION')

CREATE TABLE HOSPITALUSUARIOS(
IDHOSPITALUSUARIO INT PRIMARY KEY AUTO_INCREMENT,
FECHA DATE,
ACTIVO CHAR(1) DEFAULT '1',
IDHOSPITAL INT NOT NULL,
IDUSUARIO INT NOT NULL,
FOREIGN KEY(IDHOSPITAL)REFERENCES HOSPITALES(IDHOSPITAL),
FOREIGN KEY(IDUSUARIO)REFERENCES USUARIOS(IDUSUARIO)
)

DONDE ACTIVO:
0 = ELIMINADO(QUE SALIÓ DEL HOSPITAL)
1 = ACTIVO

INSERT INTO HOSPITALUSUARIOS(FECHA,IDHOSPITAL,IDUSUARIO)VALUES(NOW(),1,4)